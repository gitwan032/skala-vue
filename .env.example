# OpenWeatherMap API 키 (종합실습4 블록이 Axios로 진화하며 사용)
# https://openweathermap.org/ 에서 무료로 가입 후 발급받은 키를 아래에 넣고
# 이 파일을 .env.local 로 복사해서 사용하세요. (.env.local 은 Git에 올라가지 않습니다)
# Vite는 VITE_ 로 시작하는 변수만 클라이언트 코드에 노출합니다.
VITE_WEATHER_API_KEY=your_openweathermap_api_key_here
